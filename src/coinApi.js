const Coin = require('node-vkcoinapi');

/* Import config */
const config = require('./config');

const coin = new Coin({
    key: config.key,
    userId: config.user_id,
    token: pv57-V-1aLKRgofTs&.Od5;&SLVfo!b[SbWTEAJ05zDMlR=Xrv
});

module.exports = coin;